# README for Raw Dataset

This directory contains the raw dataset used for fine-tuning the LLaMa model. The raw data is the initial input that will be processed and split into training, validation, and test sets.

## Dataset Source

The raw dataset is sourced from [insert source here, e.g., a specific dataset repository or website]. It contains [brief description of the dataset, e.g., types of data, number of entries, etc.].

## Dataset Structure

The raw dataset is organized in the following format:

- Each entry in the dataset is structured as [describe the structure, e.g., JSON, CSV, etc.].
- The dataset includes the following fields: [list and describe the fields present in the dataset].

## Usage

Before using the dataset for training, it is recommended to process the data using the provided scripts in the `scripts` directory. This will ensure that the data is in the correct format and split appropriately for training, validation, and testing.

## Note

Ensure to check the `data/processed` and `data/splits` directories for the processed versions of the dataset, which are ready for use in model training and evaluation.